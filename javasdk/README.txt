This is the fabric java sdk wrapper

Then we can build our own tools based on fabric java sdk. This wrapper classes only depend on fabric java sdk, 
and also should be loaded in the HFSDK classloader.